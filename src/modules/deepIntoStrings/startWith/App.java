package modules.deepIntoStrings.startWith;

public class App {
    public static boolean startsWith(String text, String substr) {
        // BEGIN
        return text.indexOf(substr) == 0;
        // END
    }

}
